[package]
name = "sdf-common"
version = "0.13.0"
edition = "2021"
description = "Stateful Dataflow constants and common utils"
license = "Apache-2.0"
keywords = ["sdf"]

[features]
version = ["dep:semver"]

[dependencies]
anyhow = { workspace = true }
check_keyword = { workspace = true }
convert_case = { workspace = true }
regex = { workspace = true }
semver = { workspace = true, optional = true }
wit-encoder = { workspace = true }
