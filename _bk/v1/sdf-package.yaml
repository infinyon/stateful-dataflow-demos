apiVersion: 0.6.0

meta:
  name: jaq-package
  version: 0.0.1
  namespace: examples

types:
  raw:
    type: list
    items:
      type: u8

functions:
  jaq-transform:
    operator: map
    inputs:
      - name: input
        type: bytes
    output:
      type: raw

dev:
  converter: raw
